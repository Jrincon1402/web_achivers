name: Deploy to Netlify

on:
  workflow_run:
    workflows: ["CI - Validaciones y Tests"]
    types:
      - completed
    branches: [main, master, develop]
  push:
    branches: [main, master, develop]
    # El workflow_run solo despliega si el CI pasó exitosamente
    # El push despliega directamente (puedes cambiarlo según tus preferencias)

jobs:
  deploy:
    name: Desplegar a Netlify
    runs-on: ubuntu-latest
    # Solo despliega si el CI pasó exitosamente cuando se ejecuta desde workflow_run
    # O si es un push directo a la rama principal
    if: ${{ github.event.workflow_run.conclusion == 'success' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop')) }}
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Build del proyecto
        run: npm run build

      - name: Instalar Netlify CLI
        run: npm install -g netlify-cli

      - name: Desplegar a Netlify
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
        run: |
          # Si NETLIFY_SITE_ID no está configurado, crear el sitio automáticamente
          if [ -z "$NETLIFY_SITE_ID" ]; then
            echo "⚠️  NETLIFY_SITE_ID no configurado. Creando sitio automáticamente..."
            SITE_OUTPUT=$(netlify sites:create --name "${{ github.event.repository.name }}" 2>&1)
            echo "Output del comando:"
            echo "$SITE_OUTPUT"
            # Extraer el Site ID del output (buscando "Site ID:" o URL)
            NETLIFY_SITE_ID=$(echo "$SITE_OUTPUT" | grep -i "Site ID:" | sed -n 's/.*Site ID:[[:space:]]*\([a-f0-9-]*\).*/\1/p' | head -1)
            # Si no se encontró con ese formato, buscar en la URL
            if [ -z "$NETLIFY_SITE_ID" ]; then
              NETLIFY_SITE_ID=$(echo "$SITE_OUTPUT" | grep -o 'https://app\.netlify\.com/sites/[^/[:space:]]*' | sed 's|https://app\.netlify\.com/sites/||' | head -1)
            fi
            # Si aún no se encontró, intentar extraer del formato JSON que puede mostrar netlify
            if [ -z "$NETLIFY_SITE_ID" ]; then
              NETLIFY_SITE_ID=$(echo "$SITE_OUTPUT" | grep -o '"site_id":"[^"]*"' | cut -d'"' -f4 | head -1)
            fi
            if [ -z "$NETLIFY_SITE_ID" ]; then
              echo "❌ Error: No se pudo obtener el Site ID del output."
              echo "Por favor, crea el sitio manualmente en Netlify y configura NETLIFY_SITE_ID como secret."
              echo "Alternativamente, revisa el output arriba para encontrar el Site ID manualmente."
              exit 1
            fi
            echo "✅ Sitio creado con ID: $NETLIFY_SITE_ID"
            echo "⚠️  IMPORTANTE: Agrega este Site ID como secret NETLIFY_SITE_ID en GitHub para futuros deploys"
            echo "📋 Ve a: Settings > Secrets and variables > Actions y agrega NETLIFY_SITE_ID = $NETLIFY_SITE_ID"
          fi
          
          # Desplegar a producción
          netlify deploy --prod --site-id="$NETLIFY_SITE_ID" --message="Deploy desde GitHub Actions: ${{ github.sha }}"
          echo "✅ Despliegue completado exitosamente"

